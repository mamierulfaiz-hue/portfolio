---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact | Amierul Faiz" description="Let us connect. Open to discussing SOC, penetration testing, and network security opportunities.">
  <section class="mx-auto max-w-6xl px-4 py-14 sm:px-6 sm:py-20 lg:px-8 lg:py-28">
    <div class="mb-8 max-w-3xl sm:mb-10">
      <p class="text-xs uppercase tracking-[0.35em] text-cyan-400 sm:text-sm">Secure Comms</p>
      <h1 class="mt-3 text-3xl font-semibold text-white sm:text-4xl md:text-5xl">Contact Me</h1>
      <p class="mt-4 text-base leading-7 text-slate-300 sm:mt-5 sm:text-lg sm:leading-8">
        Let us connect. I am open to discussing SOC, penetration testing, and network security opportunities.
      </p>
    </div>

    <div class="grid gap-5 sm:gap-6 lg:grid-cols-[0.95fr_1.05fr]">
      <article class="rounded-2xl border border-zinc-800 bg-zinc-900/70 p-5 shadow-xl shadow-black/20 sm:rounded-3xl sm:p-7">
        <div class="flex items-center justify-between gap-3 border-b border-zinc-800 pb-4 sm:gap-4">
          <div>
            <p class="text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs">Direct Contact</p>
            <h2 class="mt-1.5 font-mono text-base font-semibold text-white sm:mt-2 sm:text-lg">System Data Panel</h2>
          </div>
          <div class="rounded-full border border-cyan-300/30 bg-zinc-950/60 px-3 py-2">
            <p class="font-mono text-xs text-cyan-200/80">ONLINE</p>
          </div>
        </div>

        <div class="mt-4 space-y-3 sm:mt-6 sm:space-y-4">
          <div class="rounded-xl border border-zinc-800 bg-zinc-950/40 p-3 sm:rounded-2xl sm:p-4">
            <p class="text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs">Email</p>
            <a
              href="mailto:m.amierulfaiz@gmail.com"
              class="mt-2 block font-mono text-sm text-emerald-300 hover:text-emerald-200 transition"
            >
              m.amierulfaiz@gmail.com
            </a>
          </div>

          <div class="rounded-xl border border-zinc-800 bg-zinc-950/40 p-3 sm:rounded-2xl sm:p-4">
            <p class="text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs">GitHub</p>
            <a
              href="https://github.com/mamierulfaiz-hue"
              target="_blank"
              rel="noreferrer"

              class="mt-2 block font-mono text-sm text-emerald-300 hover:text-emerald-200 transition"
            >
              github.com/mamierulfaiz-hue
            </a>
          </div>

          <div class="rounded-xl border border-zinc-800 bg-zinc-950/40 p-3 sm:rounded-2xl sm:p-4">
            <p class="text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs">LinkedIn</p>
            <a
              href="https://linkedin.com/in/amierulfaiz"
              target="_blank"
              rel="noreferrer"
              class="mt-2 block font-mono text-sm text-emerald-300 hover:text-emerald-200 transition"
            >
              linkedin.com/in/amierulfaiz
            </a>
          </div>

        </div>
      </article>

      <article class="rounded-2xl border border-zinc-800 bg-zinc-900/70 p-5 shadow-xl shadow-black/20 sm:rounded-3xl sm:p-7">
        <div class="flex items-center justify-between gap-3 border-b border-zinc-800 pb-4 sm:gap-4">
          <div>
            <p class="text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs">Secure Message</p>
            <h2 class="mt-1.5 font-mono text-base font-semibold text-white sm:mt-2 sm:text-lg">Encrypt and Send</h2>
          </div>
          <div class="text-xs font-mono text-cyan-200/70">TLS-ready</div>
        </div>

        <div class="mt-4 space-y-4 sm:mt-6 sm:space-y-5">
          <div>
            <label class="block text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs" for="name">Name</label>
            <input
              id="name"
              type="text"
              readOnly
              aria-readonly="true"
              tabIndex={-1}
              class="mt-2 w-full rounded-xl border border-zinc-800 bg-black/20 px-3 py-2.5 font-mono text-sm text-slate-100 placeholder:text-slate-500 outline-none transition sm:rounded-2xl sm:px-4 sm:py-3"
            />
          </div>

          <div>
            <label class="block text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs" for="email">Email</label>
            <input
              id="email"
              type="email"
              readOnly
              aria-readonly="true"
              tabIndex={-1}
              class="mt-2 w-full rounded-xl border border-zinc-800 bg-black/20 px-3 py-2.5 font-mono text-sm text-slate-100 placeholder:text-slate-500 outline-none transition sm:rounded-2xl sm:px-4 sm:py-3"
            />
          </div>

          <div>
            <label class="block text-[10px] font-mono uppercase tracking-widest text-cyan-300 sm:text-xs" for="message">Message</label>
            <textarea
              id="message"
              readOnly
              aria-readonly="true"
              tabIndex={-1}
              rows="5"
              class="mt-2 w-full resize-none rounded-xl border border-zinc-800 bg-black/20 px-3 py-2.5 font-mono text-sm text-slate-100 placeholder:text-slate-500 outline-none transition sm:rounded-2xl sm:px-4 sm:py-3"
            />
          </div>

          <div class="pt-1">
            <button
              type="button"
              disabled
              aria-disabled="true"
              class="w-full cursor-not-allowed rounded-xl bg-emerald-300/10 px-5 py-2.5 text-sm font-semibold text-emerald-200 opacity-90 border border-emerald-300/40 sm:rounded-2xl sm:py-3"
            >
              Secure Channel Active
            </button>
          </div>

        </div>

        <script>
          const parts = [
            { el: 'name', value: 'Recruiter' },
            { el: 'email', value: 'recruiter@company.com' },
            {
              el: 'message',
              value:
                'Hello Amierul. I reviewed your portfolio and I am very impressed with your SOC and Threat Detection skills. I would like to connect.',
            },
          ];

          const blinkCaret = (container) => {
            const caret = document.createElement('span');
            caret.textContent = '▌';
            caret.className = 'ml-1 inline-block animate-pulse text-emerald-300';
            container.appendChild(caret);
            return caret;
          };

          const run = async () => {
            const nameEl = document.getElementById('name');
            const emailEl = document.getElementById('email');
            const msgEl = document.getElementById('message');
            if (!nameEl || !emailEl || !msgEl) return;

            const caretHost = nameEl.parentElement;
            blinkCaret(caretHost);

            const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
            const typeInto = async (inputEl, text) => {
              inputEl.value = '';
              for (let i = 0; i < text.length; i++) {
                inputEl.value += text[i];
                await sleep(18);
              }
            };

            // loop forever
            while (true) {
              for (const part of parts) {
                const target = document.getElementById(part.el);
                if (!target) continue;
                await typeInto(target, part.value);
                await sleep(500);
              }

              await sleep(2500);

              // clear
              nameEl.value = '';
              emailEl.value = '';
              msgEl.value = '';

              await sleep(600);
            }
          };

          run();
        </script>

        <style is:global>
          input:focus,
          textarea:focus {
            outline: none;
          }
        </style>

      </article>
    </div>
  </section>
</Layout>

